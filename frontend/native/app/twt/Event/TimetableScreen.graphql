#import "./EventListItem.graphql"

query TimetableScreen($userId: String!) {
  user(id: $userId) {
    events: conferenceEvents {
      edges {
        node {
          ...EventListItem
        }
      }
    }
  }
}
