#import "./CalendarEvent.graphql"

query FetchNewVotes($userId: String!) {
  user(id: $userId) {
    votes: conferenceVotes {
      edges {
        node {
          ...CalendarEvent
        }
      }
    }
  }
}
