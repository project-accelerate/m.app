import { ConferenceEventService } from 'backend/app/conference/application/ConferenceEventService'

describe(ConferenceEventService, () => {
  describe('relevantVotes', () => {
    it('returns votes if a delegate', () => {})

    it('returns empty array if not a delegate', () => {})
  })

  describe('relevantEvents', () => {
    it('returns labour and twt events if a delegate', () => {})

    it('returns twt events if not a delegate', () => {})
  })
})
